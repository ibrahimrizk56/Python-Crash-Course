# 4-3. Counting to Twenty: Use a for loop to print the numbers from 1 to
# 20, inclusive.

#generate a list from 1 to 20 by list comprehension
numbers = list(range(1,21))
print(numbers)

#doing for loop to print from 1 to 20
for number in numbers:
    print(number)